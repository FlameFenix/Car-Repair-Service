import { Link } from "react-router-dom";
export const Administration = () => {
    return (
        <div>
            <Link to='/Contacts/Messages'>
                Contacts Messages
            </Link>
        </div>
    );
}